<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.abc.ssm.modules.sys.dao.map.UserRoleDao">
    <!-- 删除某个用户的所有角色关联 -->
    <delete id="deleteByUserId" parameterType="user">
        delete
        from sys_map_user_role
        where user_id = #{id}
    </delete>

    <!-- 批量添加 -->
    <insert id="insert" parameterType="userRole">
        insert into sys_map_user_role (user_id, role_id, id, common_remarks, common_del_flag, common_create_user_id,
        common_modify_user_id, common_create_date, common_modify_date)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId}, #{item.id}, #{item.commonRemarks}, #{item.commonDelFlag}, #{item.commonCreateUserId},
            #{item.commonModifyUserId}, #{item.commonCreateDate}, #{item.commonModifyDate})
        </foreach>
    </insert>
</mapper>